server:
  port: 9001

spring:
  application:
    name: PATIENTRECORD-SERVICE
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
        defaultZone: http://localhost:8761/eureka/
  Instance:
    hostname: localhost